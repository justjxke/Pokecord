name = "poke-discord-bridge"
main = "src/index.ts"
compatibility_date = "2026-03-31"

# Set this to the HTTPS hostname that Cloudflare Tunnel exposes for your backend.
# The install script writes the real value for you.
# [vars]
# POKE_BACKEND_ORIGIN = "https://mcp.example.com"
